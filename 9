/**
 * @param {number} x
 * @return {boolean}
 */
var isPalindrome = function(x) {
    var str  = x.toString();
    for(var i = 0;i <= str.length -1;i++){
        if(str[i] != str[str.length - 1 - i]){
            return false;
        }else{
            return true
        }
    }
}
